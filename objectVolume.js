function cube(a) {
  // write your cube function here with parameter a
  return a * a * a;
}

function rectangular(l, b, h) {
  // write your rectangular function here with parameter l, b, h
  return l * b * h;
}

console.log(cube(2));
console.log(rectangular(2,3,4));
